# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.env

# Data files — raw and weather are large downloads, exclude from git
data/raw/
data/weather/

# Processed CSVs are committed (small, reproducible outputs)
# data/processed/ is NOT excluded

# Models — committed so Render can serve them without re-training
# models/*.pkl

# Jupyter
.ipynb_checkpoints/

# Node
dashboard/node_modules/
dashboard/dist/

# OS
.DS_Store
Thumbs.db
